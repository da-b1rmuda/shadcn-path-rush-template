# Changelog

Historical versions were not found beyond the packaged `CHANGELOG.md` in `plugin/`.

## 0.1.0

- initial public MVP of `module-rush`
- typed config and manifest contracts
- module/domain discovery and manifest loading
- AST-based import analysis and architectural checks
- registry generation for Path-rush integration
- CLI commands: `init`, `create`, `generate`, `check`, `registry`, `graph`, `report`, `doctor`
- lazy-analysis with optional Vite manifest integration
- fixture and command integration tests
- base documentation set
